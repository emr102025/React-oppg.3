export default function TodoList() {
  return (
    <>
      <h1>TodoList</h1>
      <ul>
        <li>
            <input type="checkbox" checked={done} onChange={}/> 
            <button onClick={}>X</button>
        </li>
      </ul>
    </>
  );
}
